commands:
  01_install_go:
    command: |
      sudo rm -rf /usr/local/go
      curl -sL https://golang.org/dl/go1.21.0.linux-amd64.tar.gz | sudo tar -C /usr/local -xz
      echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bash_profile

      